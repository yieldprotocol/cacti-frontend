import Code from "./assets/code-brackets.svg";
import Colors from "./assets/colors.svg";
import Comments from "./assets/comments.svg";
import Direction from "./assets/direction.svg";
import Flow from "./assets/flow.svg";
import Plugin from "./assets/plugin.svg";
import Repo from "./assets/repo.svg";
import StackAlt from "./assets/stackalt.svg";
import "./styles.css";
import { Meta } from "@storybook/blocks";
import Image from "next/image";

<Meta title="cacti/Introduction" />

# Welcome to Cacti 🌵 Components 🏜️

<p>
  CactiComponents is a modular set of User Interface (UI) components designed to
  facilitate seamless integration of new platforms and protocols into
  Cacti-Chat, an AI chatbot specifically designed to simplify decentralized
  finance (DeFi) actions and allow for easy sharing across various protocols.
  These components are aimed at creating a standardized, efficient, and
  high-quality UI, thereby streamlining the development process and ensuring an
  excellent user experience.
</p>
<p>
  Each component is a self-contained, reusable piece of code that standardizes
  certain elements of the UI. Using these components, developers can construct
  complex UIs by combining smaller, manageable pieces. This modularity not only
  promotes reusability and consistency but also improves maintainability and
  scalability of the application.
</p>

{/* The current suite of CactiComponents includes:
TextResponse
The TextResponse component handles the display of textual responses from the AI chatbot. It's designed to handle standard text responses with appropriate formatting and styling.
HeaderResponse
This component is designed to create and manage headers in the chat interface. It ensures uniformity and proper styling across all headers, whether they are section headers, subheaders, or other forms of hierarchical text.
SingleLineResponse
SingleLineResponse is used to display short, succinct responses from the AI chatbot. It's perfect for answers, confirmations, or brief statements that need to be highlighted in the chat interface.
IconResponse
IconResponse deals with the rendering of icon-based responses. These could be emojis, action icons, or any other iconographic visual response. Icons can be an effective way to communicate certain actions or states without requiring extensive text.
ListResponse
This component helps display list-based responses. Whether it's a list of options, a series of steps, or any other type of ordered or unordered list, ListResponse ensures a clear and organized presentation.
ActionResponse
The ActionResponse component is designed to display actionable responses, such as buttons, links, or commands. This component is crucial in guiding the user through different actions they can take in the DeFi environment.
ImageResponse
Lastly, the ImageResponse component is for rendering image-based responses. Whether it's a diagram, an infographic, or a simple picture, this component ensures proper sizing, positioning, and resolution.
In summary, each of these components plays a unique role in building out the UI for Cacti-Chat. By leveraging these components, developers can quickly and effectively build out UIs that are robust, intuitive, and consistent. */}
